<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jeogeum">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Fragment3"></activity>
        <activity android:name=".StartActivity" />
        <activity android:name=".Set_Practice" />
        <activity android:name=".Practice" />
        <activity android:name=".PreSettingsActivity" />
        <activity android:name=".SettingActivity" />
        <activity android:name=".Showtext" />
        <activity
            android:name=".Main_WriteContent"
            android:windowSoftInputMode="adjustPan" />
        <activity android:name=".SignupActivity" />
        <activity android:name=".MyWritingActivity" />
        <activity android:name=".YourWritingActivity" />
        <activity
            android:name=".ui.login.LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>